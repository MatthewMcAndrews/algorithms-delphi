unit uSequentialSearch;

interface

function SequentialSort(
  const Domain: TArray<string>;
  const Target: string): Integer;

implementation

function SequentialSort(
  const Domain: TArray<string>;
  const Target: string): Integer;
begin
  Result := -1;
end;

end.
